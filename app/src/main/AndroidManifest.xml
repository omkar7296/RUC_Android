<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.upitt.ruc">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Splash_Screen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Landing_Activity" />
        <activity android:name=".Reg_Code" />
        <activity android:name=".Home" />
        <activity android:name=".No_Reg_Code" />
        <activity android:name=".Reg_Code_Req_Received" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme1"></activity>
    </application>

</manifest>